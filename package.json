{
  "name": "anycast",
  "version": "1.0.0",
  "main": "serveri.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "publish": "wrangler deploy --env production",
    "db:create": "wrangler d1 create anycast-db",
    "db:migrate": "wrangler d1 migrations apply anycast-db"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "author": "",
  "license": "ISC",
  "description": "Cloudflare Workers auth backend with Hono and D1"
}
